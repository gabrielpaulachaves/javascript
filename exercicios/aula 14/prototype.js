/*
function noc(nome, turno, nivel, hora, banco){
    this.nome = nome,
    this.turno = turno,
    this.nivel = nivel,
    this.hora = hora,
    this.banco = banco
}

noc.prototype.rotina = function (){
        console.log(`O(A) analista ${this.nome} está no turno da ${this.turno}, e tem conhecimentos de nível ${this.nivel}`)
}

noc.prototype.horario = function(){
    return 


}

const noc1 = new noc('Neilson', 'manhã', 'sênior')
const noc2 = new noc('Amanda','manhã','junior')
*/

/*Prototype de uma constructor: define estrutura de membros (propriedades e metodos) que sao compartilhadas pelas instancias(objetos criados a partir das constructors. Nesse exemplo, o noc1 e noc2) desse constructor */

/*defina os metodos dentro do prototype */

/*quando o objeto nao é função, ele nao tem prototype */

function loja(produto, valor, quantidade){

}


